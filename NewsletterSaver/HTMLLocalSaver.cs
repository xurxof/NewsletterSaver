using SystemWrapper.IO;

namespace NewsletterSaver {
    public sealed class HtmlLocalSaver {
        public HtmlLocalSaver(IFileWrap fileWrap) {
        
        }

        public bool Save(object remoteHtmlText, string filePath) {
            return false;
        }
    }
}